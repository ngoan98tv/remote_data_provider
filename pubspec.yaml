name: remote_data_provider
description: Help implementing providers easier with predefined abstract classes, special is for working with remote data.
version: 1.0.0
homepage: https://github.com/ngoan98tv/remote_data_provider

environment:
  sdk: ">=2.18.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.3.0
  http: ^0.13.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
